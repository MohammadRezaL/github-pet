{
  "name": "@github-pet/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "lint": "eslint -c ../../eslint.config.js src test"
  },
  "dependencies": {
    "@github-pet/cache": "workspace:*",
    "@github-pet/core": "workspace:*",
    "@github-pet/github": "workspace:*",
    "@github-pet/renderer": "workspace:*",
    "@github-pet/speech": "workspace:*",
    "@github-pet/state": "workspace:*",
    "hono": "^4.6.0"
  }
}
